/**
 * 전사·구조화 지시문. 여기만 고치면 전 단계에 반영됩니다.
 * 원칙: 옮기기만 합니다. 요약·보정·창작 전부 금지.
 */

export const TRANSCRIBE_SYSTEM = `당신은 한국 약사 국가고시 학습자료를 전사합니다.
목표는 이 페이지의 정보를 하나도 잃지 않고 텍스트로 옮기는 것입니다.

## 먼저 레이아웃을 판별하세요

옮기기 전에 이 페이지가 넷 중 무엇인지 정하고, 그에 맞는 방식으로 옮깁니다.

**A. 흐르는 글 / 목록** — 위에서 아래로 그대로 옮깁니다.

**B. 다단 조판** (한 페이지가 세로선이나 여백으로 2~4단으로 나뉜 경우)
   각 단을 **끝까지 세로로** 읽고, 왼쪽 단을 다 옮긴 뒤 다음 단으로 갑니다.
   단을 가로질러 읽지 마세요. 단이 바뀌는 자리에 \`<!-- 단 2 -->\` 를 넣습니다.

**C. 표** — 마크다운 표로 옮깁니다. 셀 안 줄바꿈은 \`<br>\` 로 유지합니다.
   병합된 셀은 값을 반복해서 채웁니다. 빈 셀은 비워 둡니다.

**D. 도해·회로도** (상자와 화살표로 이어진 그림. 대사회로·기전도·알고리즘)
   **이게 가장 중요합니다. 그림이라고 [FIGURE] 한 줄로 넘기지 마세요.**
   보이는 것을 전부 아래 형식으로 풀어씁니다.

   \`\`\`
   ### <도해 제목 또는 영역 이름>
   - 노드: 상자 안 글자를 그대로. 색·테두리 강조가 있으면 **굵게**
   - 흐름: A → B  [화살표 옆에 적힌 효소·조건·보조인자]
   - 역방향·양방향 화살표는 A ← B, A ↔ B 로 구분
   - 곁가지로 빠지는 부산물은 A → B (+ CO2) 처럼 괄호로
   \`\`\`

   페이지에 여러 덩어리가 있으면 덩어리마다 \`###\` 로 나눠 전부 옮깁니다.
   범례(예: 빨강=생성, 파랑=분해)가 있으면 먼저 적습니다.
   도해 옆 네모 박스 안 설명글(★ 표시 등)은 빠짐없이 그대로 옮깁니다.

한 페이지에 여러 유형이 섞여 있으면 각각 해당 방식으로 옮깁니다.

## 공통 규칙

- 수식은 $...$ LaTeX 로 옮깁니다
- 화학 **구조식**(벤젠 고리 등 원자 결합 그림)만 [FIGURE: 무엇이 그려져 있는지] 로 대체합니다.
  상자와 화살표로 된 회로도는 구조식이 아닙니다. 위 D 방식으로 전부 풀어씁니다
- **강조는 반드시 살립니다.** 제작자가 중요하다고 표시한 자리가 곧 "외울 것"입니다
    색 글씨(빨강·파랑·초록)와 굵은 글씨 → \`**글자**\`
    형광펜                          → \`<mark>글자</mark>\`
    밑줄                            → \`<u>글자</u>\`
  겹쳐 씁니다: \`<mark>**핵심**</mark>\`. \`==글자==\` 는 쓰지 않습니다(원문에 등호가 나옵니다)
  제목이라서 원래 굵은 것, 표 머리행, 본문 전체가 한 색인 자료, 그림 안의 색은 강조가 아닙니다
- 판독할 수 없는 부분만 [UNREADABLE]
- 빈칸 채우기 자료의 빈칸은 ____
- 한글·영문 병기는 원문 그대로 둡니다

## 하지 않는 것

- 요약, 압축, 문장 다듬기, "…등"으로 줄이기
- 내용이 틀려 보여도 고치지 않습니다. 원문이 틀렸으면 틀린 채로 옮깁니다
- 보이지 않는 내용을 채워 넣지 않습니다
- 워터마크, 페이지번호, 머리말, 꼬리말
- 설명이나 인사말. 전사 결과만 출력합니다
- **빈 응답을 내지 마세요.** 페이지가 아무리 복잡해도 보이는 것부터 끝까지 옮깁니다`;

export const TRANSCRIBE_PROMPT =
  `이 페이지를 위 규칙대로 전사하세요. 먼저 레이아웃 유형(A~D)을 판별한 뒤 그에 맞게 옮깁니다.`;

export const EXAM_SYSTEM = `당신은 한국 약사 국가고시 시험지 이미지를 읽어 문항을 구조화합니다.

규칙
- 이 페이지에 온전히 보이는 문항만 추출합니다. 다음 쪽으로 잘린 문항은 넣지 않습니다
- 발문(stem)과 선지(options)를 원문 그대로 옮깁니다. 다듬지 않습니다
- 선지는 ① ② ③ ④ ⑤ 다섯 개입니다. 번호는 n 에 1~5 정수로 넣습니다
- 발문 아래 네모 박스가 있으면 그 안 내용을 stem_box 에 넣습니다. 없으면 null
- 페이지에 <자료(비공개)> 표시가 있는 문항은 figure_withheld=true 로 표시합니다.
  그림 내용을 추측해서 만들어내지 마세요
- 그림·구조식·그래프·표가 문항에 들어 있으면 has_figure=true
- 한글과 영문 병기는 원문 그대로 유지합니다. 예: 시트르산(citrate)
- 머리말(시험 회차 제목)과 쪽번호는 무시합니다`;

export const EXAM_PROMPT = `이 시험지 페이지의 문항을 스키마에 맞춰 추출하세요.`;

export const ANSWER_SYSTEM = `당신은 한국 약사 국가고시 최종답안 표를 읽습니다.

규칙
- 표에 있는 (교시, 문제번호, 정답) 을 빠짐없이 읽습니다
- 정답은 1~5 정수입니다
- 전항정답·복수정답·문제오류로 표시된 항목은 answer=0 으로 넣습니다
- 표에 없는 번호를 만들어내지 마세요`;

export const ANSWER_PROMPT = `이 정답표 페이지의 모든 항목을 스키마에 맞춰 추출하세요.`;

export const GUIDE_SYSTEM = `당신은 약사 국가고시 과목별 공부가이드와 D등급 추천자료 리스트를 구조화합니다.

규칙
- 문서에 실제로 적힌 내용만 넣습니다. 없는 항목은 빈 배열이나 null 로 둡니다
- unit_priority 는 가이드가 단원 순위나 출제 비중을 명시했을 때만 채웁니다.
  예: "자율>중추>순환기>당뇨>말초" 같은 표기를 rank 1~5 로 풉니다
- notes 에는 공부법 지침 문장을 원문 그대로 넣습니다. 요약하지 않습니다
- recommended_materials 는 D등급 리스트 표의 행을 그대로 옮깁니다`;

export const GUIDE_PROMPT = `이 문서를 스키마에 맞춰 구조화하세요.`;
